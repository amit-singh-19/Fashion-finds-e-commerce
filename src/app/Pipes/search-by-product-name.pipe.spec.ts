import { SearchByProductNamePipe } from './search-by-product-name.pipe';

describe('SearchByProductNamePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchByProductNamePipe();
    expect(pipe).toBeTruthy();
  });
});
